# Server Configuration
PORT=80

# Database Configuration
# For local development (external host):
# DATABASE_URL=postgres://user:password@host:port/database?sslmode=disable

# For production (Docker/Easypanel internal network):
# DATABASE_URL=postgres://user:password@postgres_container_name:5432/database?sslmode=disable

DATABASE_URL=postgres://username:password@localhost:5432/superflixdb?sslmode=disable

# JWT Secret (CHANGE THIS IN PRODUCTION!)
# Generate a strong secret with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
